> [!IMPORTANT]
> **This code now lives in [homeCore-io/homeCore](https://github.com/homeCore-io/homeCore).**
>
> It was folded in on 2026-08-03, with its full history — see
> `plugins/hc-captest` there, and `docs/monorepo-plan.md` for why.
>
> This repository is archived and read-only. Its releases are kept, not
> deleted: the plugin registry still serves the versions published here, and
> deleting them would break installs of those versions. New releases are cut
> from the monorepo and tagged `<name>-v<version>`.

